# deps
node_modules
.pnpm-store

# next.js
.next
out
next-env.d.ts

# env (keep .env.example tracked)
.env
.env*.local

# build / cache
*.tsbuildinfo
.turbo

# local data (SQLite + uploaded collateral files — never commit user data)
data/*.db
data/*.db-shm
data/*.db-wal
data/*.db-journal
data/uploads/

# os
.DS_Store
*.log

# vercel
.vercel

# clerk configuration (can include secrets)
/.clerk/
.claude/

# hardhat — keep contract ABIs (lib/chain/abi.ts imports them) but
# drop heavy build-info, debug files, and the per-deployment address map.
contracts/cache/
contracts/artifacts/build-info/
contracts/artifacts/**/*.dbg.json
data/chain.json

